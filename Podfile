# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RestfulNewsSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Alamofire', '~> 4.0'
  pod 'SDWebImage', '~> 5.0'
  
  target 'RestfulNewsSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RestfulNewsSwiftUITests' do
    # Pods for testing
  end

end
